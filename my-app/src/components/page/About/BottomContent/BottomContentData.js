import React from "react";
import BottomContent from "./BottomContent";

const BottomContentData = () => {
    const BottomData = {
        titleBottom:
            "Upptäck Resan till Optikerprogrammet vid Karolinska Institutet",
    };
    return <BottomContent BottomData={BottomData} />;
};

export default BottomContentData;
